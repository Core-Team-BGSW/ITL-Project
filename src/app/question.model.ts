export interface Question {
    id: string;                // MongoDB ID
    questionId: number;        // Unique identifier for the question
    questionDescription: string; // Text of the question
  }